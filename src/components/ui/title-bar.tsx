import { ShowPreviewIcon } from "@/assets/icons";

const TitleBar = () => {
  return (
    <header className="w-full">
      <p>MARKDOWN</p>
      <span>
        <ShowPreviewIcon />
      </span>
    </header>
  );
};

export default TitleBar;
