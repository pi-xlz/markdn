import { ShowPreviewIcon } from "@/assets/icons";

const TitleBar = () => {
  return (
    <header>
      <p>MARKDOWN</p>
      <span>
        <ShowPreviewIcon />
      </span>
    </header>
  );
};

export default TitleBar;
